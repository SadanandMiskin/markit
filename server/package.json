{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "db-generate": "bunx prisma generate",
    "db-migrate": "bunx prisma migrate dev --name markit",
    "dev": "bun run compile && bun run db-migrate && bun run db-generate && bun prod/index.js",
    "compile": "tsc -b"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0",
    "prisma": "^6.2.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "6.2.1",
    "express": "^4.21.2"
  }
}